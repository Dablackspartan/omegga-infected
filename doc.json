{
  "name": "Infected / Zombies (Omegga)",
  "short_description": "Copies JSON minigame preset and loads/creates a minigame.",
  "config": {
    "minigame-name": {
      "description": "Name to load/create (defaults to 'Infected')",
      "default": "Infected"
    },
    "preset-source": {
      "description": "Filename in /plugins/infected/data (prefers *.json)",
      "default": "Infected.json"
    }
  }
}